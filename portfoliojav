const slides = document.querySelectorAll(".slide")
let index = 0

function changeSlide(){
    slides[index].classList.remove("active")
    index = (index + 1) % slides.length
    slides[index].classList.add("active")
}

setInterval(changeSlide, 4000)

function afficherDetails(nomActivite) {
  const zone = document.getElementById('affichage-details');
  
  if (nomActivite === 'Regex et Téléphonie') {
    zone.innerHTML = `
      <h2>Détails de l'activité 1</h2>
      <img src="regex.jpg" alt="Photo 1">
      <img src="photo2.jpg" alt="Photo 2">
    `;
  }
}
function afficherDetails(nomActivite) {
  const zone = document.getElementById('affichage-details');
  
  if (nomActivite === 'activite1') {
    zone.innerHTML = `
      <h2>Détails de l'activité 1</h2>
      <img src="photo1.jpg" alt="Photo 1">
      <img src="photo2.jpg" alt="Photo 2">
    `;
  }
}
